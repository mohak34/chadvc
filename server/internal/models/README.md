// Package models defines data structures
package models

// TODO: Define models for:
// - User (id, username, email, password_hash, status, etc.)
// - Server (id, name, owner_id, icon_url, etc.)
// - Channel (id, server_id, name, type, etc.)
// - Message (id, channel_id, user_id, content, timestamp, etc.)
// - File (id, channel_id, filename, size, mime_type, etc.)
